name: Set up workspace
description: >-
  Installs pnpm (version from the packageManager field) and Node.js (version from .nvmrc), restores
  the pnpm store cache and installs dependencies from the frozen lockfile. Run after checkout.

runs:
  using: composite
  steps:
    - name: Set up pnpm
      uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

    - name: Set up Node.js
      uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      with:
        node-version-file: .nvmrc
        cache: pnpm

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile
