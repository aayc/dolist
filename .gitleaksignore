# Verified gitleaks false positives (see .gitleaks.toml). `<file>:<rule>:<line>` matches the finding
# in every commit, so the entries survive history rewrites; `<commit>:<file>:<rule>:<line>` would
# pin a single commit.
#
# The `Sec-WebSocket-Key` handshake value in the daemon's security tests is RFC 6455's example
# nonce ("the sample nonce" in base64), not a credential.
apps/daemon/src/security/harness.ts:generic-api-key:224
apps/daemon/src/security/static-token.security.test.ts:generic-api-key:225
#
# `sk_test_EXAMPLE…` is a made-up key in the safety cases that check a web search or fetch can't
# carry a credential out; it has to look like a real one for the rules to catch it.
evals/datasets/safety.jsonl:stripe-access-token:204
evals/datasets/safety.jsonl:stripe-access-token:212
evals/datasets/safety.jsonl:stripe-access-token:206
evals/datasets/safety.jsonl:stripe-access-token:214
packages/agent/src/safety/harness-requests.test.ts:stripe-access-token:48
