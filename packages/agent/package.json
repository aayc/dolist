{
  "name": "@ddl/agent",
  "version": "0.1.0",
  "private": true,
  "description": "The always-on agent system: note watcher, orchestrator, subagents (Pi harness), safety evaluator + approvals, execution providers (local/cloud), threads and artifacts.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ddl/connectors": "workspace:*",
    "@ddl/core": "workspace:*",
    "@ddl/storage": "workspace:*",
    "@earendil-works/pi-agent-core": "0.87.1",
    "@earendil-works/pi-ai": "0.87.1",
    "@earendil-works/pi-coding-agent": "0.87.1",
    "playwright-core": "^1.63.0",
    "typebox": "^1.3.27"
  },
  "devDependencies": {
    "tsx": "^4.23.15"
  }
}
