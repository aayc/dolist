{
  "name": "@ddl/core",
  "version": "0.1.0",
  "private": true,
  "description": "Pure, dependency-free domain logic shared by every Daily Do List surface: vault paths, daily notes, markdown task parsing and identity tracking, agent helpers, and the daemon wire protocol's constants and URLs (its types are inferred from @ddl/contract's schemas and re-exported type-only).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "drawings:fixtures": "tsx test/drawings/generate.ts",
    "bench": "vitest bench --run --reporter=default --reporter=json --outputFile.json=bench-results.json"
  }
}
